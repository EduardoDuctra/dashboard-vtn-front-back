version: '3.8'

services:
  backend:
    build: ./API-e2g-vtn/vtn
    container_name: vtn-backend
    ports:
      - '5006:8080'

  frontend:
    build: ./interface-e2g
    container_name: vtn-frontend
    ports:
      - '5005:80'
    depends_on:
      - backend
